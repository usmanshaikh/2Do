{
  "email": {
    "type": "string",
    "unique": "Must be unique",
    "required": true
  },
  "passowrd": {
    "type": "string",
    "minLength": 8,
    "requirements": "At least one letter and one number",
    "required": true
  }
}
