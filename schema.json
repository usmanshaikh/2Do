{
  "email": {
    "type": "string",
    "unique": "Must be unique",
    "required": true
  },
  "passowrd": {
    "type": "string",
    "minLength": 8,
    "required": true,
    "requirements": "At least one letter and one number"
  }
}
