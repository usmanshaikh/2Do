import * as apiPath from "./apiPath.constants";
import * as message from "./message.constants";
import * as regex from "./regex.constants";
import * as routePath from "./routePath.constants";

const constants = {
  apiPath,
  message,
  regex,
  routePath,
};

export default constants;
